# Workspace-level environment template
# Copy the values you actually need into local .env files before running.

# Active frontend
VITE_API_BASE_URL=http://127.0.0.1:8000

# Backend
HOST=127.0.0.1
PORT=8000
